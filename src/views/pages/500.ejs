<%# Estado 500 genérico: nunca se le muestra el stack trace a la clienta
    (§8.1 / threat matrix). El detalle real queda en console.error del
    servidor (ver app.js). %>
<section class="mx-auto max-w-[1400px] px-4 py-24 text-center">
  <h1 class="mb-4 text-2xl font-semibold">Algo salió mal</h1>
  <p class="mb-6 text-textMuted">Estamos teniendo un problema técnico. Probá de nuevo en un momento.</p>
  <a href="/" class="rounded px-4 py-2 text-sm font-semibold text-text" style="background: var(--brand-gradient);">Volver al inicio</a>
</section>
