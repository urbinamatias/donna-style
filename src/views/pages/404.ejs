<%# Estado 404 genérico (§5.4 threat matrix: slug inexistente → 404 legible,
    nunca un stack trace). %>
<section class="mx-auto max-w-[1400px] px-4 py-24 text-center">
  <h1 class="mb-4 text-2xl font-semibold">No encontramos esa página</h1>
  <p class="mb-6 text-textMuted">Puede que el link esté mal escrito o que la página ya no exista.</p>
  <a href="/" class="rounded px-4 py-2 text-sm font-semibold text-text" style="background: var(--brand-gradient);">Volver al inicio</a>
</section>
